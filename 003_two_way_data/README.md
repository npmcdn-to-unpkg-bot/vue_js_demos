#send data to the server via post get a response and change page accordingly

to setup ```npm install```

to run ```node app.js```

navigate to *localhost:8080* via a browser of your choice.
send data via the form of your choice.
